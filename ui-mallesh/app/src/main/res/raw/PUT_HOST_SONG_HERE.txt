Place your host_song.wav here.

Required format:
  - Sample rate: 44,100 Hz
  - Channels: Mono (1 channel)
  - Bit depth: 16-bit PCM
  - Format: WAV (RIFF)

Convert with ffmpeg:
  ffmpeg -i your_song.mp3 -ar 44100 -ac 1 -sample_fmt s16 host_song.wav

Then delete this file.
